/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'plus.unsplash.com',
      'www.pngitem.com',
      'encrypted-tbn0.gstatic.com',
    ],
  },
};

export default nextConfig;
